#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# 🎓 SPIKE ACADEMIC PLATFORM - PRE-PUSH HOOKS
# Enterprise-grade validation before push to remote

echo "🚀 Spike Academic Platform - Pre-push validation starting..."

# ================================================================================================
# 🔍 COMPREHENSIVE CHECKS
# ================================================================================================

echo "📋 Running comprehensive pre-push checks..."

# Get the current branch
current_branch=$(git branch --show-current)
remote_branch="origin/$current_branch"

# Check if we're pushing to main/master branch
if [ "$current_branch" = "main" ] || [ "$current_branch" = "master" ]; then
  echo "⚠️  Pushing to $current_branch branch - running extra validation..."
  EXTRA_VALIDATION=true
else
  EXTRA_VALIDATION=false
fi

# ================================================================================================
# 🔧 TYPE CHECKING
# ================================================================================================

echo "🔧 Running TypeScript type checking..."

npm run type-check

if [ $? -ne 0 ]; then
  echo "❌ TypeScript type checking failed! Please fix type errors before pushing."
  exit 1
fi

echo "✅ TypeScript type checking passed"

# ================================================================================================
# 🧪 TESTING
# ================================================================================================

echo "🧪 Running unit tests..."

npm run test:unit

if [ $? -ne 0 ]; then
  echo "❌ Unit tests failed! Please fix failing tests before pushing."
  exit 1
fi

echo "✅ Unit tests passed"

# ================================================================================================
# 🌍 HEBREW/RTL TESTING (MANDATORY per CLAUDE.md)
# ================================================================================================

echo "🌍 Running Hebrew/RTL specific tests..."

# Run Hebrew component tests if they exist
if [ -f "package.json" ] && grep -q "test:hebrew" package.json; then
  echo "🧪 Running Hebrew component tests..."
  npm run test:hebrew
  
  if [ $? -ne 0 ]; then
    echo "❌ Hebrew component tests failed! Please fix Hebrew/RTL issues before pushing."
    exit 1
  fi
  
  echo "✅ Hebrew component tests passed"
fi

# Run RTL lint checks if available
if [ -f "package.json" ] && grep -q "lint:rtl" package.json; then
  echo "🔍 Running RTL compliance checks..."
  npm run lint:rtl
  
  if [ $? -ne 0 ]; then
    echo "❌ RTL compliance checks failed! Please fix RTL issues before pushing."
    exit 1
  fi
  
  echo "✅ RTL compliance checks passed"
fi

# ================================================================================================
# 🔍 LINTING
# ================================================================================================

echo "🔍 Running ESLint..."

npm run lint

if [ $? -ne 0 ]; then
  echo "❌ ESLint checks failed! Please fix linting errors before pushing."
  exit 1
fi

echo "✅ ESLint checks passed"

# ================================================================================================
# 🎨 FORMATTING CHECK
# ================================================================================================

echo "🎨 Checking code formatting..."

npm run format:check

if [ $? -ne 0 ]; then
  echo "❌ Code formatting check failed! Please run 'npm run format' to fix."
  exit 1
fi

echo "✅ Code formatting check passed"

# ================================================================================================
# 🔒 SECURITY AUDIT (for main branch)
# ================================================================================================

if [ "$EXTRA_VALIDATION" = true ]; then
  echo "🔒 Running security audit..."
  
  npm run security:audit
  
  if [ $? -ne 0 ]; then
    echo "⚠️  Security vulnerabilities detected! Please review and fix."
    echo "You can continue pushing but should address these issues soon."
  else
    echo "✅ Security audit passed"
  fi
fi

# ================================================================================================
# 📦 BUILD TEST (for main branch)
# ================================================================================================

if [ "$EXTRA_VALIDATION" = true ]; then
  echo "📦 Testing production build..."
  
  npm run build
  
  if [ $? -ne 0 ]; then
    echo "❌ Production build failed! Please fix build errors before pushing to main."
    exit 1
  fi
  
  echo "✅ Production build test passed"
fi

# ================================================================================================
# 🎓 ACADEMIC PLATFORM SPECIFIC VALIDATION
# ================================================================================================

echo "🎓 Running academic platform specific validation..."

# Check for Hebrew content integrity
if git diff --name-only HEAD $remote_branch 2>/dev/null | xargs grep -l "[א-ת]" 2>/dev/null; then
  echo "🌍 Validating Hebrew content changes..."
  
  # Check for broken Hebrew text
  for file in $(git diff --name-only HEAD $remote_branch 2>/dev/null | xargs grep -l "[א-ת]" 2>/dev/null); do
    if [ -f "$file" ]; then
      # Check for mixed RTL/LTR issues (simplified check)
      if grep -E "[a-zA-Z][א-ת]|[א-ת][a-zA-Z]" "$file" > /dev/null; then
        echo "⚠️  Potential RTL/LTR mixing in $file - please review"
      fi
    fi
  done
  
  echo "✅ Hebrew content validation passed"
fi

# Validate API routes haven't broken
if git diff --name-only HEAD $remote_branch 2>/dev/null | grep "app/api/" > /dev/null; then
  echo "🔗 API routes changed - validating structure..."
  
  # Check that API routes export proper HTTP methods
  for api_file in $(git diff --name-only HEAD $remote_branch 2>/dev/null | grep "app/api/.*route\.(ts|js)$"); do
    if [ -f "$api_file" ]; then
      if ! grep -E "export\s+(async\s+)?function\s+(GET|POST|PUT|DELETE|PATCH)" "$api_file" > /dev/null; then
        echo "⚠️  API route $api_file might not export HTTP methods properly"
      fi
    fi
  done
  
  echo "✅ API routes validation passed"
fi

# ================================================================================================
# 📊 PERFORMANCE CHECKS (for main branch)
# ================================================================================================

if [ "$EXTRA_VALIDATION" = true ]; then
  echo "📊 Running performance checks..."
  
  # Check bundle size (simplified)
  if [ -d ".next" ]; then
    bundle_size=$(du -sh .next 2>/dev/null | cut -f1)
    echo "📦 Current bundle size: $bundle_size"
  fi
  
  # Check for performance anti-patterns in staged files
  if git diff --name-only HEAD $remote_branch 2>/dev/null | xargs grep -l "useState.*\[\].*useState" 2>/dev/null; then
    echo "⚠️  Multiple useState calls detected - consider useReducer or Zustand"
  fi
  
  echo "✅ Performance checks completed"
fi

# ================================================================================================
# 🌐 DEPLOYMENT READINESS (for main branch)
# ================================================================================================

if [ "$EXTRA_VALIDATION" = true ]; then
  echo "🌐 Checking deployment readiness..."
  
  # Check for environment variables
  if [ ! -f ".env.example" ]; then
    echo "⚠️  .env.example file missing - create one for deployment guidance"
  fi
  
  # Check for required deployment files
  REQUIRED_FILES=("package.json" "next.config.js" "tsconfig.json")
  for file in "${REQUIRED_FILES[@]}"; do
    if [ ! -f "$file" ]; then
      echo "❌ Required file missing for deployment: $file"
      exit 1
    fi
  done
  
  echo "✅ Deployment readiness check passed"
fi

# ================================================================================================
# ✅ SUCCESS
# ================================================================================================

echo ""
echo "✅ All pre-push checks passed!"
echo "🚀 Spike Academic Platform - Ready for push to $current_branch"

if [ "$EXTRA_VALIDATION" = true ]; then
  echo "🎯 Main branch validation completed successfully"
fi

echo ""
